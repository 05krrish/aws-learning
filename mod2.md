today i learned about types of instances and launching one 
                        EC2 (Elastic cloud compute)
-- types of instances are ->
                            ~general purpose 
                            --for vast variety with general purpose 
                            ~compute optimised
                            ~memory optimized 
                            ~storage optiised 
                            ~acclerated compute